package interfaces.CarExample;

public interface cc{
    public void cc();
    // we can write static method but need to declare the body
    // we cannot write finale method in interface
    // any variable that is written in interface is final and static
}

