import java.util.ArrayDeque;
import java.util.Deque;

public class Dequeu {
    // you can insert and remove from front and back also
    // it  does not have restrictions
    public static void main(String[] args) {
        Deque<Integer> deque=new ArrayDeque<>();

        deque.addFirst(45);
        deque.addLast(36);
    }
}
